export const config = {
  // Replace this with your deployed Vercel server URL after deployment
  // Example: 'https://workout-viewer-server.vercel.app'
  serverUrl: __DEV__ ? 'http://localhost:3000' : 'https://your-server-deployment.vercel.app',
};
